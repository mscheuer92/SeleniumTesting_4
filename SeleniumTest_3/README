4/5/2021
  -Added two new classes
  -Currently working out how to include a nested function into POM.
  -Currently the project opens ebay, searches, sorts by a specified price, and returns the number available
  -The nested function will separate the integer from the string of available, and then input that number into the quantity location

 4/5/2021
 	-Added two new classes
 	-Nested function in addToCart functions correctly
 	-Go to cart, and then remove item
 
 This program is complete.