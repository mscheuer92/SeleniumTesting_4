This Python program opens Google Chrome and navigates to the Google site.
It then searches for a parameter and loads that page
Then it searches for an element on the page and clicks that link before completing the test.

I also have it set up to generate an HTML test report for the test cases.